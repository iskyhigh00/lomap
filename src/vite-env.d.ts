/// <reference types="vite/client" />

/** Injected by `vite.config.ts`'s `define` from `package.json`'s `version`
 * field at build time — see `TopBar.tsx` for where it's shown. */
declare const __APP_VERSION__: string
